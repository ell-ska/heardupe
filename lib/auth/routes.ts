export const apiAuthPrefix = '/api/auth'

export const publicRoutes = [
  '/',
  '/become-beta-user',
  '/become-beta-user/submitted',
]

export const authRoutes = ['/auth/login', '/auth/error']

export const defaultLoginRedirect = '/'
