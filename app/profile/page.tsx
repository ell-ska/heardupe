import { LogoutButton } from '@/components/AuthButtons'

const Profile = () => {
  return (
    <main className='main flex-grow'>
      Profile
      <LogoutButton />
    </main>
  )
}

export default Profile
